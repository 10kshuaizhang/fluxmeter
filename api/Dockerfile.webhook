FROM python:3.11-slim
WORKDIR /app
COPY api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY api/webhook_worker.py api/webhook_deliver.py .
CMD ["python", "webhook_worker.py"]
