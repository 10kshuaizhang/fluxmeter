apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
  labels:
    app: fluxmeter-api
spec:
  selector:
    app: fluxmeter-api
  ports:
    - port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
